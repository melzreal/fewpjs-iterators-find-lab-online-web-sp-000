const testVar = {}

function testFunc() {
  return "hi"
}

const superbowlWin = (objArr) => {
  let result = objArr.filter(x => { 
    if (x.result ==="W") 
    return x.year 
  })
  
  return result
}